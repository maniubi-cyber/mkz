## 慕课栈

技术栈：Spring Cloud Alibaba、Spring Boot、RocketMQ、Redis、MySQL、FastAPI、LangChain、Chroma

项目描述：慕课栈是一个分布式在线教育平台，涵盖视频观看、优惠券发放、点赞评论、排行榜等核心业务模块。项目引入AI能力构建智能助手，实现了对话记忆管理、工具调用与人工审批机制，提升了系统的自动化水平与操作安全性。

项目成果：

- **课程模块：**
  - 针对课程热点数据高并发读取场景下的缓存击穿风险，设计 Caffeine + Redis 双层缓存架构。利用 Caffeine 的 refreshAfterWrite 异步刷新机制对 Redis 压力进行兜底，有效避免了热点 Key 过期瞬间大量请求穿透至数据库，显著提升了接口的响应速度与稳定性。
  - 排行榜基于 Redis ZSet 实现实时排序，按赛季做冷热分层——当季热数据常驻 Redis 支撑毫秒级查询，往季冷数据归档至 MySQL 释放内存，由XXL-Job定期归档。

- **AI模块：**
  - 针对多轮对话中上下文窗口溢出导致早期信息丢失的问题，设计滑动窗口 + 摘要压缩策略。最近 N 轮对话原文保留，超出部分由 LLM 动态压缩为语义摘要存入 Redis并checkpoint 持久化。在防止上下文撑爆的前提下，既节省 Token 消耗又保证长对话的连贯性。
  - 基于 ReAct 循环实现"思考→调用工具→观察结果→再决策"的多步推理，并在输入侧拦截提示词注入与越权意图。工具调用返回结构化JSON数据，解析失败时自动携带格式纠正提示词重试、工具执行异常（如接口超时）不中断循环，而是将错误信息作为结果回传，由 LLM 自行决定降级回复或切换工具。
  - 工具按风险程度分为三级：低级（课程查询、排行榜查看）直接执行，并对工具返回结果进行校验，防止编造不存在的信息；中级（优惠券领取、点赞）执行前校验幂等；高级（课程上下架、违规评论处理）触发 Human-in-the-Loop 审批进行二次确认。

- **优惠券模块：**
  - 为解决并发场景下的超发与超领问题，基于 Redisson + AOP 自定义注解实现零侵入式防重控制；通过 Redis Lua 脚本将库存校验、用户限额校验与库存扣减合并为原子操作，配合数据库乐观锁作为最终兜底方案，双重保障优惠券发放的准确性与一致性。

- **消息模块：**
  - 课程数据变更、优惠券发放等操作通过 RocketMQ 异步解耦，大幅提升系统响应速度。同时建立本地消息表 + XXL-Job 定时扫描补偿机制，确保消息在异常情况下的最终一致性，有效降低了模块间的耦合度。


## 共汇

技术栈：Spring Boot、Elasticsearch、Redis、MySQL、FastAPI、LangChain、Qdrant、RAGAS

项目描述：共汇是基于 RAG 的团队文档智能问答与协作平台，覆盖文档入库、语义检索、智能问答全链路，支持评测驱动的持续迭代；同时集成 OT 协同编辑与多源数据聚合。

项目成果：

- **RAG 检索与生成：**
  - 文档入库采用父子切块策略（父块保留段落语义、子块切至句子级），入库时 LLM 自动提取摘要与标签写入元数据。子块 embedding 存入 Qdrant 做检索入口，命中后回溯父块提供完整上下文。文档编辑后通过版本号 diff 定位变更段落，仅对变更块重新切块与 embedding（增量索引），避免全量重建。
  - 针对单一检索路径的召回盲区（向量漏精确关键词、关键词漏语义改写），设计 ES BM25 + Qdrant 向量双路召回，RRF 融合排序取 top-k，叠加元数据权限过滤确保用户仅命中有权限的文档。用户 query 进入检索前经 LLM 轻量改写（指代消解、口语化表述修正），提升复杂 query 下的召回率。
  - 生成回答通过 SSE 流式输出并携带来源标注（文档名 + 段落位置），供用户溯源验证。基于 RAGAS + Langfuse 构建评测与可观测体系，覆盖 faithfulness / answer relevancy / context precision 三项指标，每次调整切块策略、检索参数或 prompt 后跑评测集对比，确保改动可量化、可回退。

- **工程基础：**
  - 文档详情页聚合多源数据（内容、作者、权限、浏览数），通过 CompletableFuture 异步编排 + 自定义线程池并行查询，响应时间从 350ms 降至 120ms。搜索基于 Elasticsearch + IK 中文分词，支持相关度排序与高亮，10 万级数据量下响应 60ms。
  - 多人协同编辑基于 ot.js + WebSocket 实现，前端将文本变更转为 retain/insert/delete 原子操作，后端负责操作广播与版本号管理，通过 transform 机制自动解决并发冲突。
