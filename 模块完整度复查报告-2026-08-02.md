# mkz 其他模块完整度复查报告（2026-08-02）

> 审计方式：4 个 Explore 代理并行只读复查 + 关键 P0 抽验（AliPayService 退款查询、AbstractIdempotentListener 幂等时序均已在本地代码核实）。
> 范围：简历点名的课程/优惠券/消息模块（验证声称能力是否真实正确）+ 未点名模块（用户/认证/支付/发号/数据/媒体/搜索/考试/评价/网关/公共）。
> 前序已复查并修复：mkz-learning、mkz-live、mkz-trade（见 08-01 记录）。

## 总览

| 模块 | 结论 | High 发现数 |
|---|---|---|
| mkz-course | 有真实实现，但排行榜/缓存 4 处 High | 4 |
| mkz-promotion | 简历能力真实，Lua 并发/时间单位 3 处 High | 3 |
| mkz-message | 本地消息表闭环✅，补偿重发丢 keys 1 处 High | 1 |
| mkz-user | 健康（关注/粉丝缺失，简历未宣称） | 0 |
| mkz-auth | 健康（JWT RSA256/白名单/feign 中继真实） | 0 |
| mkz-pay | 回调验签/幂等✅，退款链路 2 处 High（条件写反+失败误判成功） | 2 |
| mkz-unqid | 空壳（src 已删除，仅编译产物） | 1 |
| mkz-data | MQ 消费闭环✅，统计日期口径错位 3 处 High | 3 |
| mkz-media | MinIO/VOD 上传✅，删除不删云端 1 处 High（另 3 处条件性） | 1 |
| mkz-search | 健康（ES 同步/查询闭环✅） | 0 |
| mkz-exam | 判分闭环✅，重复提交幂等缺失 1 处 + 主观题 0 分 | 1 |
| mkz-remark | **重灾区**：点赞明细不落库、实时 MQ 被注释、整个 ServiceImpl 注释空壳 | 3 |
| mkz-gateway | 鉴权✅，sms 路由 id 重复 + 网关限流未生效 | 2 |
| mkz-common | 基础设施✅，幂等基类"先标记后消费"失败丢消息（影响全部消费者） | 1 |

## 修复状态汇总（2026-08-02 晚间，全部闭环）

| 优先级 | 发现数 | 状态 | 修复 commit |
|---|---|---|---|
| P0（资金/崩溃/数据） | 11 | ✅ 全部修复 | `cc204284`、`eecbe212`、`529a170d` |
| P1（功能失效/明显缺陷） | 6 | ✅ 全部修复 | `24e1d035`、`b543f9ed`（#13 并入 `cc204284`） |
| P2（收尾/一致性） | 6 | ✅ 闭环（2 条核查无需改动） | `2fb63477` |

修复后全量 reactor `mvn compile` 验证通过（EXIT=0）。附带修复：`.gitignore` 裸 `job` 条目误伤 mkz-data 的 XXL-Job 业务目录（LogStatisticsJob/LogAnalysisJob 补录入库）。

---

## P0（数据正确性 / 资金 / 崩溃级）—— ✅ 11 条已全部修复

### 1. mkz-pay `AliPayService.java:135` 退款查询条件写反
> 状态：✅ **已修复** commit `cc204284`
`if (ResponseChecker.success(response))` 成功被当失败 → 退款单永远停在 UN_KNOWN 反复轮询。
对照 `queryPayOrderStatus:77` 的正确写法 `!success`。修复：`if (!ResponseChecker.success(response))`。

### 2. mkz-pay `RefundOrderServiceImpl.java:248-257` 第三方退款失败误判成功
> 状态：✅ **已修复** commit `cc204284`
`checkRefundOrder` 只要状态变化就发 `RefundResultDTO.success()`，第三方退款失败时业务端误判成功。修复：按 refundResponse 实际状态构造 SUCCESS/FAILED（对照 `NotifyServiceImpl:127`）。

### 3. mkz-common `AbstractIdempotentListener.java:24-35` 失败消息永久丢失（影响 learning/live/promotion/search 全部消费者）
> 状态：✅ **已修复** commit `cc204284`（MessageIdempotentUtil 增 remove，失败回滚幂等键）
"先标记后消费"：`checkAndMark` 先写幂等键，`doConsume` 抛异常时键已存在且不移除 → MQ 重投后被幂等跳过 → **失败消息永久丢失**。
修复：`MessageIdempotentUtil` 增 `remove(businessId)`（del key），`consume` 的 catch 中先 `remove` 再 rethrow，保证失败可重试、成功不重复。

### 4. mkz-course `RankingArchiveJobHandler.java:62-67` 归档误删实时数据
> 状态：✅ **已修复** commit `cc204284`（归档显式排除 "current"）
currentSeasonId≠"current" 时，扫描会命中仍被写入的 `ranking:course:current` → 实时数据被删除且重复入库（history 表无唯一键）。修复：归档条件显式排除字面量 "current"。

### 5. mkz-course `CourseRankingServiceImpl.java:186+84` switchCurrentSeason 塞假成员 "init" 抛异常
> 状态：✅ **已修复** commit `cc204284`（删 "init" 假成员 + 读取/归档防御跳过非法成员）
`getSeasonRanking` 对 "init" 执行 `Long.valueOf("init")` → NumberFormatException。修复：删除假成员或判空。

### 6. mkz-promotion Lua 时间单位不一致（receive_coupon.lua:7 / exchange_coupon.lua:14）
> 状态：✅ **已修复** commit `eecbe212`（统一 epoch 秒）
`CouponServiceImpl:174/337` 存毫秒、`CouponStockJobHandler:79` 存 ISO 串，Lua 用 `time()[1]`（秒）比较 → 毫秒路径结束校验永不触发、ISO 路径 tonumber 得 nil 脚本报错。修复：统一为秒（或毫秒）。

### 7. mkz-promotion Lua `hincrby` 先自增后比较（receive_coupon.lua:10 / exchange_coupon.lua:17）
> 状态：✅ **已修复** commit `eecbe212`（先读后比较再自增）
超限请求也永久累加用户领取计数且无回滚 → 限额数据被污染。修复：先比较后自增，或失败时 hincrby -1 回滚。

### 8. mkz-promotion `CouponStockJobHandler.java:130-132` 用 Redis 库存覆盖 DB 不可变 total_num
> 状态：✅ **已修复** commit `eecbe212`（只同步 issue_num）
破坏 `issue_num < total_num` 乐观校验 → 后续发放可能全拒。修复：只同步 issue_num。

### 9. mkz-message `LocalMessageServiceImpl.java:81` 补偿重发不带 KEYS
> 状态：✅ **已修复** commit `eecbe212`（补偿路径带 keys 发送）
补偿路径用 `RocketMqHelper.sendSync(topic, tags, content)`（无 keys 重载）→ 消费者 businessId=null → 所有补偿消息共用 `mq:consumed:null` 幂等键，仅首条被消费、其余被误判重复跳过。修复：改用带 keys 的 `sendSync(topic, tags, keys, msg)` 重载（已在 RocketMqHelper 提供）。

### 10. mkz-data 统计日期口径错位（3 处同源）
> 状态：✅ **已修复** commit `529a170d`（saveLogs 增 reportDate 参数，LogStatisticsJob 传数据所属日期）
- `BusinessReportServiceImpl:50 + LogStatisticsJob:37-39`：Job 统计"昨日"日志但 `saveLogs` 落 `reportDate=now()` → 报表日期错位一天，且按"今天"去重导致同日重跑被跳过。修复：用数据发生日（昨日）作为 reportTime。
- `DataHandler:85-96 + OrderAnalysisJobHandler:98-99`：写"今日"面板的订单额/单量实为默认 `minusDays(1)` 的昨日数据 → 今日看板滞后一天。修复：统一为今日实时口径。

### 11. mkz-remark 点赞明细永不落库（数据丢失）
> 状态：✅ **已修复** commit `529a170d`（liked/unliked 恢复 liked_record 落库/删除）
`LikedRecordRedisServiceImpl.java:113-136`：点赞/取消仅写 Redis SET，`liked_record` 落库代码整段注释 → Redis 清空即全量丢失；`:71-89` 实时 MQ 发送被注释，仅靠 10s 定时 `LikedTimesCheckTask` 兜底。修复：恢复 MQ 发送 + 消费端落库（建议切 RocketMqHelper + AbstractIdempotentListener，与全站一致）。

---

## P1（功能失效 / 明显缺陷）—— ✅ 6 条已全部修复

### 12. mkz-course `TwoLevelCacheManager` refreshAfterWrite 从未触发（简历宣称的 Caffeine 异步刷新实际未生效）
> 状态：✅ **已修复** commit `24e1d035`（LoadingCache + get(key)，refreshAfterWrite 真实生效；删死 Bean）
`CaffeineConfig:31` 配置了 refreshAfterWrite 但读路径全程 `getIfPresent/put`，从不调 `cache.get(key)`，CacheLoader 永不执行；`rankingLocalCache/categoryLocalCache` 两个 Bean 无人注入（死代码）。修复：读路径改用 `cache.get(key)`（配合 CacheLoader），或删除该宣称。

### 13. mkz-course `CourseRankingServiceImpl:39-45/62-64` 排行榜 key 读写分裂
> 状态：✅ **已修复** commit `cc204284`（updateScore 写当前赛季 key，随 P0 批次1 一并修复）
`updateScore` 无参版写死 `ranking:course:current`，查询按 `CURRENT_SEASON_ID` 拼接 → `switchCurrentSeason` 后读写分裂、实时榜失效；3 参 `updateScore` 无调用方。修复：读写统一走赛季 key 解析。

### 14. mkz-remark `LikedRecordServiceImpl.java:1-117` 整段注释空壳 + 技术栈混用
> 状态：✅ **已修复** commit `b543f9ed`（删空壳，链路统一 RocketMQ + 幂等）
整个 ServiceImpl 被注释成残留；链路用 `RabbitMqHelper` + learning `@RabbitListener`，未走约定 RocketMqHelper + AbstractIdempotentListener，无幂等/补偿。修复：删除空壳，统一 RocketMQ 链路。

### 15. mkz-exam `ExamRecordServiceImpl.java:244-306` 交卷幂等缺失
> 状态：✅ **已修复** commit `24e1d035`（finished 幂等校验）
`saveExamRecordDetails` 未校验 `record.finished`，重复提交重复插入 `exam_record_detail` 明细。修复：提交前校验 finished 或先删旧明细再落库。

### 16. mkz-media `MediaController.java:68-79` 删除不删云端文件
> 状态：✅ **已修复** commit `24e1d035`（deleteMediaById 先删云端再删本地）
`deleteMedia/deleteMedias` 仅 MP `removeById`，未调 `mediaStorage.deleteFile` 删 VOD/OSS 云端文件 → 视频永久残留（`MediaServiceImpl.deleteMedia` 已实现云端删除但未被复用）。修复：删除时补云端删除。

### 17. mkz-gateway `bootstrap.yml:41-50` sms 路由 id 重复
> 状态：✅ **已修复** commit `24e1d035`（sms-ws 唯一 id + Path=/ws/**）
HTTP 与 WS 两条路由同 id `sms`，后定义覆盖前者 → HTTP 私信或 WebSocket 其一失效。修复：改为唯一 id（sms-http / sms-ws）。

---

## P2（收尾 / 一致性）—— ✅ 6 条已闭环（含 2 条核查无需改动）

### 18. mkz-gateway 网关限流未生效
> 状态：✅ **已修复** commit `2fb63477`（RateLimitGlobalFilter 固定窗口限流，零依赖）
pom 无 sentinel-gateway 依赖、无 RequestRateLimiter/自定义限流 filter。修复：引入 Sentinel 网关限流或自定义 filter。

### 19. mkz-exam `ExamRecordServiceImpl.java:294` 主观题判分 TODO
> 状态：✅ **已修复** commit `2fb63477`（主观题置"待人工评阅"，不再恒 0 分）
简答/论述恒 0 分，判分仅比对 answer 文本。修复：主观题转人工/半自动评分落库。

### 20. mkz-data `LogAnalysisJob.java:51-55` 用户画像结果转发被注释（断链）
> 状态：✅ **已修复** commit `2fb63477`（删除死发送，清理死代码）
`mqHelper.send(DATA_ANALYSIS_LOG_KEY)` 整段注释，`logAnalysisResult` 只打日志不落库不转发 → 画像→搜索链路断裂。修复：恢复发送或补充消费端/删除死代码。

### 21. mkz-unqid 空壳
> 状态：✅ **无需改动**（不在父 POM modules、git 未跟踪孤儿目录，构建层面已下线）
src 源码已删除（仅 target 编译产物，基于 COSID 非手写雪花），`UnqidClient` 无业务引用。简历未宣称发号能力则建议从父 pom 下线该模块；若宣称需恢复源码并接入业务（订单号等）。

### 22. mkz-gateway dev 环境缺 `/ais`(aigc)、`/uns`(unqid) 路由（local 才有），且两模块 src 为空壳
> 状态：✅ **无需改动**（全仓无 /ais /uns 路由，死路由不存在）
修复：补齐路由或下线模块。

### 23. 其它（视简历口径）
> 状态：⏭️ **视简历口径**：user 关注/粉丝简历未点名，未实现（如需宣称需新增关注关系表+接口）；pay @Lock private 问题有乐观锁兜底，记录在案
- mkz-user 关注/粉丝全仓库无实现（简历未点名，若需宣称需新增关注关系表+Redis 粉丝数）。
- mkz-pay `@Lock` 标注在 private 方法上不生效（NotifyServiceImpl:255、RefundOrderServiceImpl:99），已有乐观锁/幂等兜底，可后续修正。

---

## ✅ 已核实为真实且正确（无需改动）

- **mkz-course**：Caffeine+Redis 双层缓存读路径真实接入（CourseServiceImpl:181/455）；排行榜 ZSet + 赛季冷热分层 + XXL-Job 归档骨架真实。
- **mkz-promotion**：`@Idempotent` + AOP + Redisson 防重真实（IdempotentAspect:62-113）；Lua 脚本真实加载调用（UserCouponLuaServiceImpl:88/103）；乐观锁兜底真实（CouponMapper:41-65）。
- **mkz-message**：LocalMessage 实体/SQL 一致；落库→afterCommit 投递→补偿→死信→清理闭环完整；promotion 领券/兑换确实走该机制。
- **mkz-auth**：JWT RSA256 签发、refresh-token JTI 存 Redis 校验+登出作废；网关 AccountAuthFilter → USER_HEADER → UserInfoInterceptor 写 UserContext 并清理；feign 中继真实；白名单 14 条；密钥无硬编码（jks 加载）。
- **mkz-pay**：微信 v3/支付宝回调验签、金额比对、幂等（状态判断+乐观锁）真实；pay 与 trade 的 MQ 消费闭环真实。
- **mkz-data**：订单/课程/日志 MQ 消费闭环真实；InfluxDB 自定义 ORM 真实查询；XXL-Job 注册真实。
- **mkz-media**：MinIO 上传/秒传/分片/合并真实；腾讯 VOD 上传/播放 JWT 签名 + 事件轮询真实；File 表 key/bucket 持久化。
- **mkz-search**：课程上下架 RocketMQ 索引同步闭环真实（本地消息表发送→CourseEventListener 幂等消费→ES）；ES 查询/高亮/分页/补全真实；订单支付/退款增量更新销量真实。
- **mkz-exam**：题库/考试闭环真实，客观题判分真实（ExamRecordServiceImpl:285-290）；Feign 链不断。
- **mkz-gateway**：AccountAuthFilter 鉴权 + 白名单真实；`/lv/` 直播路由存在。
- **mkz-common**：全局异常/响应包装/MyBatis 自动填充（MyBatisAutoFillInterceptor）/Redisson 多模式适配/限流切面（LimiterConfig+RateLimiterAspect）/本地消息表补偿，全部真实注册生效；建库脚本齐全。

## 建议修复顺序
1. **P0 批次 1（资金/崩溃）**：#1 #2 #3 #5 —— 退款误判 ×2、幂等丢消息、赛季切换崩溃
2. **P0 批次 2（数据正确性）**：#4 #6 #7 #8 #9 —— 排行榜误删、Lua ×2、库存覆盖、补偿丢 keys
3. **P0 批次 3（数据丢失）**：#10 #11 —— 统计日期错位、点赞不落库
4. **P1**：#12 ~ #17
5. **P2**：#18 ~ #23（unqid/aigc 空壳建议直接下线或补齐，视简历口径）
