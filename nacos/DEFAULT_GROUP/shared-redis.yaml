spring:
  redis:
    host: ${mkz.redis.host:192.168.150.101}
    password: ${mkz.redis.password:123321}
    lettuce:
      pool:
        max-active: ${mkz.redis.pool.max-active:8}
        max-idle: ${mkz.redis.pool.max-idle:8}
        min-idle: ${mkz.redis.pool.min-idle:1}
        max-wait: ${mkz.redis.pool.max-wait:300}